<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
  </head>

  <body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
      <div class="container">
        <%= link_to 'Blog', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to posts_path, class: 'nav-link btn btn-outline-primary me-2' do %>
                  <i class="bi bi-file-text"></i> Posts
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to edit_user_registration_path, class: 'nav-link btn btn-outline-secondary me-2' do %>
                  <i class="bi bi-person-circle"></i> Profile
                <% end %>
              </li>
              <li class="nav-item">
                <%= button_to destroy_user_session_path, method: :delete, class: 'nav-link btn btn-danger', data: { turbo: false } do %>
                  <i class="bi bi-box-arrow-right"></i> Logout
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: 'nav-link btn btn-outline-primary me-2' do %>
                  <i class="bi bi-person-plus"></i> Sign Up
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: 'nav-link btn btn-primary' do %>
                  <i class="bi bi-box-arrow-in-right"></i> Login
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</html>